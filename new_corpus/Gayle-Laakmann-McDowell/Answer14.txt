result.toString() : input;}If you want to  you can remove the usage of the "previous" variable above. It's not actually strictly necessary. You can also look forward instead of backward: 1 2 3 4 5 6 7 8 91011121314String compress(String input) { StringBuffer result = new StringBuffer(); int frequency = 0; for (int i = 0; i < input.length(); i++) { frequency++; if (i + 1 >= input.length() || input.charAt(i) != input.charAt(i + 1)) { result.append(input.charAt(i)); result.append(frequency); frequency = 0; } } return result.length() < input.length() ?