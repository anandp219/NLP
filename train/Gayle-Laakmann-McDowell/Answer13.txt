result.toString() : input;}That wasn't so hard.Now  let's fix the readability issue with the nested loops. 1 2 3 4 5 6 7 8 9101112131415161718192021String compress(String input) { StringBuffer result = new StringBuffer(); int frequency = 1; char previous = input.charAt(0); for (int i = 1; i < input.length(); i++) { if (input.charAt(i) == previous) { frequency++; } else { result.append(input.charAt(i - 1)); result.append(frequency); previous = input.charAt(i); frequency = 1; } } result.append(previous); result.append(frequency); return result.length() < input.length() ?